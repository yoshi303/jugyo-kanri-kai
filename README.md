# 授業管理（公開配布版）

初期データを含まない、ブラウザ配布用のPWAです。利用者の入力データは各端末のブラウザ内だけに保存されます。

公開するには、このフォルダ内の全ファイルを静的Webホスティング（GitHub Pages、Cloudflare Pages、学校のWebサーバー等）へアップロードしてください。公開後、利用者はSafariでURLを開き、共有メニューの「ホーム画面に追加」を選ぶことでiPadアプリのように使えます。

注意：端末内保存のため、別端末との自動同期は行いません。端末交換前には、アプリの設定画面からバックアップファイルを書き出してください。
